wget "https://ftp.ebi.ac.uk/pub/databases/Rfam/CURRENT/Rfam.clanin"
wget "https://ftp.ebi.ac.uk/pub/databases/Rfam/CURRENT/Rfam.cm.gz"
gunzip Rfam.cm.gz
cmpress Rfam.cm

ID=$1
# seqkit
seqkit=/tempor/merlat/exogaptwo/cache/preprocessing/seqkit_stats/${ID}/${ID}.stats
sum_len=$(tail -n +2 $seqkit |cut -f 5)
# Z = sum_len *2 / 1,000,000 (with number after .)
Z=$(echo "scale=6; $sum_len * 2 / 1000000" | bc)

cmscan -Z $Z --cut_ga --rfam --nohmmonly --tblout ${ID}.tblout --fmt 2 --clanin /tempor/merlat/exogaptwo/data/rfam/Rfam.clanin /tempor/merlat/exogaptwo/data/rfam/Rfam.cm $genome > ${ID}.cmscan
